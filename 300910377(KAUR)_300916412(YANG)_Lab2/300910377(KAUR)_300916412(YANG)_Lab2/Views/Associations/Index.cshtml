@model IEnumerable<_300910377_KAUR__300916412_YANG__Lab2.Models.Association>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VideoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CommentId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VideoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AssociationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AssociationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AssociationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
